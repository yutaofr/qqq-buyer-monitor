# v9.0 User Stories And Tasks

> 版本: v9.0
> 状态: Approved for implementation
> 日期: 2026-03-28
> 对应 SRD: `docs/v9.0_risk_management_matrix.md`
> 对应 ADD: `docs/v9.0_risk_management_add.md`

## Epic 1: 风险控制 contract 收口到 `target_beta + qld_share_ceiling`

### US-1.1: 作为系统，我要在每个风险状态下输出 `qld_share_ceiling`

- 价值：把 “总 beta 控制” 与 “杠杆 ETF 参与上限” 分开
- 验收：
  - `RiskDecision` 包含 `qld_share_ceiling`
  - stress 单调收缩

任务：

1. 修改 `src/models/risk.py`
2. 修改 `src/engine/risk_controller.py`
3. 更新 `tests/unit/test_risk_controller.py`

### US-1.2: 作为系统，我要用 beta ceiling + `qld_share_ceiling` 双过滤选择候选

- 价值：同 beta 下允许有限 `QLD`，但禁止越权杠杆路径

任务：

1. 修改 `src/engine/allocation_search.py`
2. 如有需要，补充 `src/engine/runtime_selector.py` 注释或 helper
3. 更新 `tests/unit/test_allocation_search_v8.py`
4. 更新 `tests/integration/test_v7_runtime_pipeline.py`

## Epic 2: 内部 materialization 与 v9 叙事对齐

### US-2.1: 作为系统，我要把 `target_beta` 连续 materialize 为 ceiling-aware 的内部路径

- 价值：消除 “`beta<=1` 就必须无 `QLD`” 的旧假设

任务：

1. 修改 `src/engine/execution_policy.py`
2. 更新 `tests/unit/test_execution_policy.py`

### US-2.2: 作为回测系统，我要让 backtest 与 live 使用同一 materialization 逻辑

- 价值：确保用户入口与回测入口决策路径一致

任务：

1. 修改 `src/main.py`
2. 修改 `src/backtest.py`
3. 更新 `tests/integration/test_signal_alignment_backtests.py`
4. 更新 `tests/integration/test_backtest_v8_pipeline.py`

## Epic 3: registry 与文档对齐到 v9 contract

### US-3.1: 作为认证系统，我要提供 stress-aware 的 limited-`QLD` 候选

- 价值：让 checked-in registry 与 v9 架构一致，而不是只停留在文档层

任务：

1. 修改 `tests/fixtures/candidate_registry_v7.json`
2. 修改 `data/candidate_registry_v7.json`
3. 更新 `tests/unit/test_candidate_registry.py`

### US-3.2: 作为用户，我要在 README / 回测报告 / SRD 衍生文档中看到一致叙事

- 价值：避免对系统 contract 的再度误解

任务：

1. 修改 `README-CN.md`
2. 修改 `docs/backtest_report.md`
3. 保持 `docs/v9.0_risk_management_matrix.md`、`docs/v9.0_risk_management_add.md`、`docs/v9.0_risk_management_spt.md` 一致

## Epic 4: QA 与交付闭环

### US-4.1: 作为 QA，我要有与验收标准一一对应的测试用例

任务：

1. 编写 `docs/v9.0_qa_test_plan.md`
2. 运行 unit / integration / acceptance / backtest 验证
3. 形成书面测试结论

### US-4.2: 作为架构师，我要对 spec、architecture、tests、docs 做最终交付复核

任务：

1. 形成 code review 结论
2. 形成 architect review 结论
3. 形成 QA black-box 结论
4. 形成用户 acceptance checklist
