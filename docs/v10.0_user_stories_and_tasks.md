# v10.0 User Stories And Tasks

> 版本: v10.0
> 状态: Approved for implementation
> 日期: 2026-03-29
> 对应 SRD: `docs/v10.0_cycle_factor_srd.md`
> 对应 ADD: `docs/v10.0_cycle_factor_add.md`

## Epic 1: 周期制度层

### US-1.1 作为系统，我要把估值、信用、广度、趋势整合为 `cycle_regime`

任务：

1. 新增 `src/models/cycle.py`
2. 新增 `src/engine/cycle_factor.py`
3. 新增 `tests/unit/test_cycle_factor.py`

### US-1.2 作为风控系统，我要把 `CycleDecision` 合并到 `RiskDecision`

任务：

1. 修改 `src/engine/risk_controller.py`
2. 更新 `tests/unit/test_risk_controller.py`
3. 更新 `tests/integration/test_v8_linear_pipeline.py`

## Epic 2: runtime / backtest 一致化

### US-2.1 作为 live runtime，我要在主链路里暴露 `cycle_regime`

任务：

1. 修改 `src/main.py`
2. 修改 `src/models/__init__.py`
3. 修改 `src/store/db.py`
4. 更新 `tests/integration/test_main.py`
5. 更新 `tests/unit/test_v7_signal_result.py`
6. 更新 `tests/unit/test_v7_persistence.py`

### US-2.2 作为 backtest，我要用相同周期逻辑回放历史

任务：

1. 修改 `src/backtest.py`
2. 更新 `tests/integration/test_backtest_v8_pipeline.py`

## Epic 3: 候选与实现路径

### US-3.1 作为候选池，我要支持 pure-QQQ、recovery limited-QLD、capitulation limited-QLD

任务：

1. 修改 `tests/fixtures/candidate_registry_v7.json`
2. 修改 `data/candidate_registry_v7.json`
3. 更新 `tests/integration/test_v7_runtime_pipeline.py`

### US-3.2 作为 advisory materialization，我要在 `qld_share_ceiling=0` 时退化为 pure-QQQ

任务：

1. 更新 `tests/unit/test_execution_policy.py`
2. 如有需要调整 `src/engine/execution_policy.py`

## Epic 4: Narrative 对齐

### US-4.1 作为用户，我要在 CLI / Web / Discord 看到统一的 v10 决策链

任务：

1. 修改 `src/output/cli.py`
2. 修改 `src/output/web_exporter.py`
3. 修改 `src/output/discord_notifier.py`
4. 修改 `src/web/public/index.html`
5. 更新 `tests/unit/test_web_exporter.py`
6. 更新 `tests/unit/test_discord_notifier.py`

## Epic 5: 交付闭环

### US-5.1 作为 QA，我要有单独的 QA 与 backtest 计划

任务：

1. 编写 `docs/v10.0_qa_plan.md`
2. 编写 `docs/v10.0_backtest_plan.md`
3. 跑测试、回测、UAT

### US-5.2 作为架构师，我要创建 implementation plan 并完成 PR

任务：

1. 编写 `docs/plans/2026-03-29-v10.0-cycle-factor-implementation.md`
2. 完成 commit
3. 创建 GitHub PR
