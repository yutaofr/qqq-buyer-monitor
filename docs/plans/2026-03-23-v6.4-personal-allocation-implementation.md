# v6.4 Personal Allocation Implementation Plan (COMPLETED)

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Build a personal-investor allocation engine that searches SRD-approved bands and selects optimal (QQQ:QLD:Cash) ratios based on a 30% drawdown budget.

---

## 1. Model Layer (AC-1) - COMPLETED 2026-03-23
- [x] Update `CurrentPortfolioState` to support `.from_env()` with fallback to 100% cash.
- [x] Update `TargetAllocationState` with `to_dict`/`from_dict` for persistence.
- [x] Ensure `SignalResult` carries the full audit context (reality vs ideal).

## 2. Candidate Search Layer (AC-6) - COMPLETED 2026-03-23
- [x] Create `src/engine/allocation_search.py`.
- [x] Define `_SRD_BANDS` matrix (QQQ, QLD, Cash) per state.
- [x] Implement `generate_candidates(state)`.
- [x] Implement `find_best_allocation(state, scores)` with **AC-5 Hard Threshold (MDD <= 0.30)**.

## 3. Backtest Scoring Layer (AC-3, AC-4) - COMPLETED 2026-03-23
- [x] Extend `src/backtest.py` with `score_candidates(ohlcv, state, candidates)`.
- [x] Implement Multi-Asset NAV simulation (Cash + QQQ + QLD).
- [x] Implement T+0 Daily Rebalancing logic to ensure Beta Fidelity.
- [x] Implement **Measured NAV Integrity (AC-3)** (Replacing hardcoded 1.0).

## 4. Strategy Selection Layer (AC-5) - COMPLETED 2026-03-23
- [x] Implement sorting logic in `find_best_allocation`:
    1. Filter by **AC-4 Beta Fidelity** (Deviation $\le 0.05$).
    2. Filter by **AC-5 30% MDD Budget**.
    3. Rank by CAGR (desc) $\rightarrow$ MDD (asc) $\rightarrow$ Turnover (asc).
- [x] Integrate selector into `src/engine/aggregator.py` via **Live Path Scoring**.

## 5. Persistence & Reporting Layer (AC-7) - COMPLETED 2026-03-23
- [x] Update `src/store/db.py` to persist `current_portfolio` and `target_allocation`.
- [x] Implement lazy migration for historical records.
- [x] Update `src/output/cli.py` to show "Reality" vs "Ideal" and **Search Rationale**.

## 6. Regression & Hardening - COMPLETED 2026-03-23
- [x] Fix legacy tests broken by SRD band changes.
- [x] Verify AC-2 (QLD=0 in defensive states).
- [x] Verify AC-4 (Realized Beta deviation $\le 0.05$).
- [x] Run full regression suite (184 tests passed).
- [x] Run full sample backtest (1999-2026).

---

## Final Status
Version 6.4 is fully implemented, addressing all expert review points. Live path scoring is active, 30% MDD budget is enforced, and NAV integrity is measured.
