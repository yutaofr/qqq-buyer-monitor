# v9.0 Delivery Reviews

> 版本: v9.0
> 日期: 2026-03-28
> 对应 SRD: `docs/v9.0_risk_management_matrix.md`
> 对应 ADD: `docs/v9.0_risk_management_add.md`
> 对应 SPT: `docs/v9.0_risk_management_spt.md`
> 对应 QA: `docs/v9.0_qa_test_plan.md`

## 1. Code Review

结论：`PASS`

检查项：

- spec compliance：通过
- architecture alignment：通过
- live / backtest path parity：通过
- maximum test coverage on changed scope：通过
- backtest non-regression：通过
- docs alignment：通过

关键核对点：

- `RiskDecision` 已输出 `qld_share_ceiling`
- `allocation_search` 已同时约束 `target_exposure_ceiling` 与 `qld_share_ceiling`
- `target_allocation_from_beta()` 已支持 `beta < 1.0` 时的有限 `QLD`
- `Deployment Controller` 仍然只描述新增现金买入 `QQQ` 的节奏
- `src/main.py` 与 `src/backtest.py` 对 advisory hard constraints 的处理一致

## 2. Architect Review

结论：`PASS`

决策、约束与不变量复核：

- 对外 contract 是 `target_beta + deployment_state`，不是唯一资产比例
- `QLD` 可以出现在任何 beta 档位，但必须 obey `qld_share_ceiling`
- `RISK_REDUCED` 与 `RISK_DEFENSE` 允许有限 `QLD`
- `RISK_EXIT` 强制 `QLD == 0`
- `Deployment Controller` 不参与存量 beta 计算，不授予 `QLD`
- 用户入口与回测入口均复用 `risk -> selection -> advisory` 决策链

## 3. QA Review

结论：`PASS`

黑盒与回归证据：

- `pytest tests/unit -q`
- `pytest tests/integration -q`
- targeted `ruff check` on v9.0 touched files
- `python scripts/run_signal_acceptance_report.py --save-dir artifacts/v9_review`
- `python -m src.backtest --mode portfolio`

结果摘要：

- unit：通过
- integration：通过
- beta envelope：`floor_respected=true`, `cap_respected=true`
- deployment alignment：`exact=100.00%`, `within_one_step=100.00%`
- crisis unauthorized breaches：`0`
- portfolio MDD：`-28.5%`

## 4. User Acceptance Checklist

结论：`READY FOR USER ACCEPTANCE`

用户应重点检查：

1. `docs/v9.0_risk_management_matrix.md` 是否准确表达 `target_beta` first contract
2. `docs/backtest_report.md` 与 `artifacts/v9_review/` 的指标是否一致
3. `RISK_REDUCED` / `RISK_DEFENSE` 是否允许有限 `QLD`
4. `CRISIS` 下是否仍保证 `QLD=0` 且 deployment 不抬高存量 beta
5. portfolio backtest 是否继续满足 `MDD <= 30%`

## 5. Residual Notes

- 仓内存在历史遗留的 repo-wide Ruff debt；本次 v9.0 以 changed scope targeted lint gate 作为工程验收口径。
- 这不是对 v9.0 行为的功能回归，而是既有代码库质量债务，应在独立治理任务中处理。
