# v10.0 Cycle Factor Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Introduce a v10.0 cycle decision layer that gates target beta and QLD usage across runtime, backtest, and output surfaces.

**Architecture:** Keep the v9 pipeline intact, insert a new `CycleDecision` between `Tier-0` and `Risk`, and propagate the new regime through persistence, reporting, and narrative layers. Historical breadth and trend are rebuilt from price so live and backtest share one classifier.

**Tech Stack:** Python 3.12, pytest, Ruff, SQLite JSON persistence, static web export, Discord webhook output

---

### Task 1: Document v10.0 contract

**Files:**
- Create: `docs/v10.0_cycle_factor_srd.md`
- Create: `docs/v10.0_cycle_factor_add.md`
- Create: `docs/v10.0_cycle_factor_spt.md`
- Create: `docs/v10.0_user_stories_and_tasks.md`
- Create: `docs/v10.0_qa_plan.md`
- Create: `docs/v10.0_backtest_plan.md`

**Step 1:** Write the docs before touching production code.
**Step 2:** Keep the product contract explicit: `target_beta` remains the external contract; `cycle_regime` governs `QLD`.

### Task 2: Write failing cycle-domain tests

**Files:**
- Create: `tests/unit/test_cycle_factor.py`

**Step 1:** Add `CAPITULATION`, `RECOVERY`, `MID_CYCLE`, `LATE_CYCLE`, `BUST`, `UNQUALIFIED` tests.
**Step 2:** Run `pytest tests/unit/test_cycle_factor.py -q` and verify failure.

### Task 3: Implement cycle-domain model and engine

**Files:**
- Create: `src/models/cycle.py`
- Modify: `src/models/__init__.py`
- Create: `src/engine/cycle_factor.py`

**Step 1:** Add the data model.
**Step 2:** Add the classifier.
**Step 3:** Run `pytest tests/unit/test_cycle_factor.py -q` and make it pass.

### Task 4: Write failing integration tests for risk/runtime/backtest/output

**Files:**
- Modify: `tests/unit/test_risk_controller.py`
- Modify: `tests/integration/test_v7_runtime_pipeline.py`
- Modify: `tests/integration/test_backtest_v8_pipeline.py`
- Modify: `tests/integration/test_main.py`
- Modify: `tests/unit/test_web_exporter.py`
- Modify: `tests/unit/test_discord_notifier.py`
- Modify: `tests/unit/test_v7_signal_result.py`
- Modify: `tests/unit/test_v7_persistence.py`

**Step 1:** Add assertions for `cycle_regime`, QLD gating, and v10 narrative order.
**Step 2:** Run focused pytest commands and verify red.

### Task 5: Integrate cycle decision into production path

**Files:**
- Modify: `src/engine/risk_controller.py`
- Modify: `src/main.py`
- Modify: `src/backtest.py`
- Modify: `src/store/db.py`
- Modify: `src/output/cli.py`
- Modify: `src/output/web_exporter.py`
- Modify: `src/output/discord_notifier.py`
- Modify: `src/web/public/index.html`

**Step 1:** Thread `cycle_regime` through runtime and backtest.
**Step 2:** Update persistence and output traces.
**Step 3:** Re-run focused tests until green.

### Task 6: Update candidate fixtures

**Files:**
- Modify: `tests/fixtures/candidate_registry_v7.json`
- Modify: `data/candidate_registry_v7.json`

**Step 1:** Add pure-QQQ and cycle-aware limited-QLD candidates.
**Step 2:** Re-run runtime/backtest tests.

### Task 7: Full verification

**Step 1:** Run `pytest tests/unit -q`
**Step 2:** Run `pytest tests/integration -q`
**Step 3:** Run `ruff check src tests`
**Step 4:** Run `python -m src.backtest --mode portfolio`
**Step 5:** Review failures and fix until all pass.

### Task 8: Commit and PR

**Step 1:** Create a focused commit with v10.0 cycle-factor changes.
**Step 2:** Push the branch.
**Step 3:** Create a GitHub PR with verification evidence.
